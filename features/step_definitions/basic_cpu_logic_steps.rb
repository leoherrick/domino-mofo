Given /^the line has a single domino \(the (\d+)\-(\d+)\)$/ do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Given /^its the cpu's turn to play$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^the cpu has a (\d+)\-(\d+), a (\d+)\-(\d+), and a (\d+)\-(\d+) in its hand$/ do |arg1, arg2, arg3, arg4, arg5, arg6|
  pending # express the regexp above with the code you wish you had
end

When /^the cpu plays$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^the domino played should have been the (\d+)\-(\d+)$/ do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Then /^the (\d+) of the (\d+)\-(\d+) should become an open\-end on the line$/ do |arg1, arg2, arg3|
  pending # express the regexp above with the code you wish you had
end

Then /^the (\d+) of the (\d+)\-(\d+) should remain an open\-end on the line$/ do |arg1, arg2, arg3|
  pending # express the regexp above with the code you wish you had
end

Then /^the cpu should score (\d+) points$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Then /^the cpu's turn should be over$/ do
  pending # express the regexp above with the code you wish you had
end
